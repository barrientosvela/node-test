<%- include("template/cabecera.ejs", {tituloWeb: "Lista de Pokemones" })%>

    <div class="container">
        <!-- Será el listado de pokemon -->
        <div class="container">
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Nombre</th>
                        <th scope="col">Tipo</th>
                        <th scope="col">Descripción</th>
                        <th scope="col">Ataque</th>
                        <th scope="col">Acción</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (arrayPokemon.length> 0) { %>
                        <!--Haremos el foreach para recorrer el array-->
                        <% arrayPokemon.forEach(pokemon=> { %>
                            <tr>
                                <td>
                                    <%= pokemon.nombre %>
                                </td>
                                <td>
                                    <%= pokemon.tipo %>
                                </td>
                                <td>
                                    <%= pokemon.descripcion %>
                                </td>                                
                                <td>
                                    <%= pokemon.ataque%>
                                </td>
                                <td>
                                    <a class="btn btn-warning btn-sm"
                                        href="pokemon/<%= pokemon.id %>">Editar/Eliminar</a>
                                </td>
                            </tr>
                            <% }) %>
                                <% } %>
                </tbody>
            </table>
            <a href="crear"><button type="button" class="btn btn-success">Añadir</button></a>
        </div>
    </div>

    <%- include("template/footer.ejs")%>